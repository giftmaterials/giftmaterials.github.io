# Empty GitHub Pages site

This folder is a minimal GitHub Pages site. Upload its contents to the root of a repository named `<your-account>.github.io`, then enable Pages from the `main` branch and the repository root.

Replace `index.html` with your own page and add any images, videos, PDFs, CSS, or JavaScript files you need.
